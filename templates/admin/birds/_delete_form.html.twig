<form method="post" action="{{ path('app_bird_admin_delete', {'id': bird.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bird.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
